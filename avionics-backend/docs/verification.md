# v0.1 验证记录

日期：2026-09-15。平台：Windows x64，MSYS2 MinGW-w64 g++ / gcc 15.2.0，CMake 3.28.1，Ninja 1.11.1，Debug 构建。

执行命令：`./scripts/build-mingw.ps1 -RunTests -RunDemo`。最终运行退出码为 0；20 个构建步骤完成，最终构建日志没有 warning/error。CTest 注册了 1 个集成测试程序，程序内部执行下列场景，全部通过，用时 1.30 秒。

| 场景 | 结果 |
|---|---|
| C ABI 头文件由 C 编译器编译 | 通过 |
| 有符号小端值转换、解码器注册 | 通过 |
| 阈值事件、无效值处理、新旧代次隔离、回放原代次隔离 | 通过 |
| 确定性构造队列满、拒绝计数、关闭后拒绝提交 | 通过 |
| 注入记录失败，故障可见、排空失败、后续输入被拒绝 | 通过 |
| 短载荷与未知协议分别计数，原始采集流程继续 | 通过 |
| 归档读写、禁止覆盖、CRC 损坏、记录头/载荷截断 | 通过 |
| 双实例共享库句柄、ABI 不兼容、错误配置拒绝 | 通过 |
| 新插件启动失败，旧实例恢复且获得新代次 | 通过 |
| 连续 20 次替换，另一实例代次不变且继续产出 | 通过 |
| stop 返回后无新增回调，停止后可重新启动 | 通过 |
| 最后实例移除后，宿主持有的动态库句柄释放 | 通过 |
| 模拟库切换为回放库，中文归档路径可用 | 通过 |
| 回放载荷、协议、通道、标记、序号、采集时间与来源逐条一致 | 通过 |

最后一次演示结果：

```text
primary replaced; secondary generation=2 -> 2, emitted=11 -> 20
loaded_plugins_after_remove=0
accepted=43 completed=43 dropped=0 samples=43
decode_errors=0 unsupported=0 healthy=1 retained_events=3
```

演示归档：`runs/demo_159350219620700.avbus`。

测试归档目录：`build-gcc-debug/test-output/159349020672200/`。日志里的 `V:/` 是构建脚本临时映射到本项目目录的盘符，执行结束已经解除；对应文件仍位于项目实际目录。

构建、测试和演示日志分别位于 `build-gcc-debug/build.log`、`test.log`、`demo.log`。源码与关键产物的 SHA-256 快照见 `verification.json`；后续重新构建可能改变日志和二进制哈希，该文件仅对应本次验证。

这些结果证明模拟条件下的功能行为，不是物理总线验证、吞吐上限、实时性、长时间稳定性或零丢帧保证。未在 Linux 编译，未连接任何真实总线，未验证设备带电插拔，也未实现业务级多参数关联分析。
